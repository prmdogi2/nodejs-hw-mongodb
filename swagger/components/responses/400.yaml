description: Bad Request - Gönderilen veri doğrulama (Joi) kurallarına uymuyor
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: string
          example: error
        code:
          type: integer
          example: 400
        message:
          type: string
          example: '"name" length must be at least 3 characters long'